"""K 近邻教案。"""

LESSON = {
    "name": "K 近邻",
    "tagline": "不训练，只记住所有数据；预测时问「离你最近的 K 个邻居怎么说」。",
    "story": (
        "1951 年 Fix 与 Hodges 给出最近邻分类的理论基础，1967 年 Cover 与 Hart 证明了一件惊人的事："
        "样本量趋于无穷时，1-近邻的错误率不超过贝叶斯最优错误率的两倍 —— 一个「什么都不学」的算法"
        "居然有这种保证。代价是它把计算全压到了预测时刻，这也是它今天仍活在推荐召回、"
        "异常检测、向量检索（kNN + 近似最近邻索引）里的原因。"
    ),
    "formula": {
        "text": "d(x, xᵢ) = (Σ|xⱼ − xᵢⱼ|^p)^(1/p)　　ŷ = majority{yᵢ : i ∈ N_K(x)}　　wᵢ = 1/d(x, xᵢ) 时 ŷ = argmax_c Σ_{i∈N_K, yᵢ=c} wᵢ",
        "vars": [
            {"sym": "d", "zh": "距离：p=2 欧氏、p=1 曼哈顿、p→∞ 切比雪夫"},
            {"sym": "N_K(x)", "zh": "离 x 最近的 K 个训练样本集合"},
            {"sym": "K", "zh": "邻居数：唯一的「模型复杂度」旋钮，K 越小越灵活"},
            {"sym": "wᵢ", "zh": "距离加权时的权重，让近邻说话更大声"},
        ],
    },
    "intuition": [
        "kNN 没有「训练」阶段，它把训练数据本身当作模型。所以它的偏差/方差不来自参数假设，"
        "而完全来自 K：K=1 时决策边界会绕过每一个噪声点（方差爆炸、边界像碎片）；"
        "K=n 时所有点都属于同一个类（偏差爆炸、退化成多数类）。误差 vs K 的 U 形曲线就是本页最核心的图。",
        "距离度量的选择不是小事。代入 x=(1, 9) 与 x'=(2, 1)：欧氏距离 √(1+64)=8.06，"
        "曼哈顿 1+8=9，切比雪夫 max(1,8)=8。若第二个特征是「星期几」这类无意义量纲，"
        "它就直接主导了邻居的选取。所以 kNN 对特征缩放极其敏感，必须先标准化/归一化，"
        "甚至做特征加权或改用马氏距离。",
        "维度灾难在这里是可算的：单位超立方体内随机取点，最近邻距离的期望随维度 d 上升而趋近 1，"
        "而最远-最近距离的相对差 (max−min)/min 趋近 0 —— 所有点都「一样远」，"
        "「邻居」这个概念失去意义。经验上超过 10~20 维就应先降维（PCA/t-SNE 页可以对照看）。",
        "3D 视图里高度 = 投票结果，所以边界是「台阶」。把 K 从 1 调到 41，你会看到台阶边缘"
        "从锯齿变成缓坡：这就是方差被平均掉的几何图像。",
    ],
    "derivation": [
        {
            "title": "① 找一个点的预测",
            "body": "训练集：(0,0)→A、(0,1)→A、(1,0)→B、(1,1)→B、(2,2)→B。查询 x=(0.9, 0.1)，K=3。"
                    "欧氏距离分别 0.906、1.273、0.141、0.906、2.195 → 最近三个是 B(0.141)、A(0.906)、B(0.906) "
                    "→ 多数 B，预测 B。改成距离加权 w=1/d：B 得 1/0.141 + 1/0.906 = 7.09 + 1.10 = 8.19，"
                    "A 得 1/0.906 = 1.10 → B 压倒性胜出——因为最近的邻居投了决定性一票。",
            "formula": "预测 = argmax_c Σ_{i∈N_K, yᵢ=c} 1/d(x,xᵢ)",
        },
        {
            "title": "② 偏差-方差的解析图像",
            "body": "K=1 时，模型的决策函数在每个训练点附近剧烈跳动，换一批训练数据边界就完全变形 → 高方差、低偏差。"
                    "K→n 时决策函数变成常数（多数类）→ 零方差、最高偏差。"
                    "对 K 个邻居取平均，独立噪声的方差按 1/K 缩小，但同时把邻域内真实的类别差异也抹平了（偏差上升）。"
                    "最优 K 通常出现在「刚开始明显欠拟合」的位置，用交叉验证挑它。",
            "formula": "Err(K) ≈ Bias²(K) + Variance(K)/K + 不可约噪声",
        },
        {
            "title": "③ 维度的诅咒算一次",
            "body": "在 [0,1]^d 中随机撒 n 个点。用第 n 维均匀化的近似：最近邻距离 ≈ (1/(n+1))^(1/d)。"
                    "d=2、n=100 → 0.099；d=10 → (1/101)^0.1 = 0.63；d=50 → 0.912。"
                    "维度升到 50，「最近」的邻居距离已经接近整个空间尺度，邻居不邻居都没有区分度了。"
                    "要维持同样的相对距离，样本量必须随维度指数增长。",
            "formula": "d_min ≈ (1/(n+1))^(1/d) → 1（当 d→∞）",
        },
        {
            "title": "④ 复杂度与加速",
            "body": "暴力检索：每个查询点要和全部 n 个样本比一次，O(n·d)，预测 10 万条高维向量代价巨大。"
                    "近似最近邻（ANN）用 KD-Tree（低维有效，d>15 退化）、Ball Tree、或"
                    "HNSW/LSH/IVF-PQ 这类图/哈希方法，把检索降到近似 O(log n)，"
                    "用一点召回率换巨大速度提升。这也是「向量数据库」的核心技术。",
            "formula": "暴力 O(n·d)　vs　KD-Tree 平均 O(d·log n)",
        },
    ],
    "terms": [
        {"term": "惰性学习", "full": "Lazy / Instance-based Learning", "explain": "训练阶段不建模，全部计算推迟到预测时。"},
        {"term": "欧氏距离", "full": "L2 Norm", "explain": "p=2 的 Minkowski 距离；对量纲敏感。"},
        {"term": "曼哈顿距离", "full": "L1 / City-block", "explain": "各维绝对差之和，对单维离群值更稳健，高维下更有区分度。"},
        {"term": "Minkowski", "full": "Minkowski Distance", "explain": "p 范数距离族，p→∞ 退化为切比雪夫距离。"},
        {"term": "维度灾难", "full": "Curse of Dimensionality", "explain": "维度升高时所有点趋于等距，基于距离的方法失效。"},
        {"term": "KD-Tree", "full": "k-dimensional Tree", "explain": "按维交替切分的空间索引，低维加速近邻查询。"},
        {"term": "ANN", "full": "Approximate Nearest Neighbor", "explain": "牺牲少量精度换检索速度：HNSW、LSH、IVF-PQ。"},
        {"term": "曲率参数 K", "full": "K in kNN", "explain": "唯一的复杂度旋钮；与核带宽、树深度是同一种「容量」的不同写法。"},
    ],
    "presets": [
        {"id": "noisy", "label": "K=1（记住噪声）", "params": {"k": 1, "weighted": 0}},
        {"id": "smooth", "label": "K=21（边界平滑）", "params": {"k": 21, "weighted": 1}},
        {"id": "city", "label": "曼哈顿距离 + 加权", "params": {"k": 9, "p": 1, "weighted": 1}},
    ],
    "pitfalls": [
        "现象：加了新特征后准确率暴跌 → 未缩放的新特征主导了距离。对策：所有特征统一标准化，"
        "或对特征加权，或用树模型/朴素贝叶斯这类不做距离运算的模型交叉验证。",
        "现象：类别不平衡时少数类几乎预测不出来 → 多数决天然偏向多数类。对策：距离加权 + K 调小 + 分层评估（每类召回率）。",
        "现象：训练集变大后预测越来越慢 → kNN 的推理成本随 n 线性增长。对策：先降维/抽样，或改 ANN 索引；"
        "线上服务通常改用「K-Means 压缩原型 + kNN」。",
        "现象：用交叉验证挑 K 时忘了每次重拟合 → 没有参数可学，看起来无所谓；但如果你同时做了缩放，"
        "scaler 必须只在训练折上 fit，否则数据泄漏。",
        "现象：把 K 设为偶数导致二分类平票 → argmax 结果依赖实现顺序。对策：二分类用奇数 K，或加距离权重彻底消除平票。",
    ],
    "seeAlso": ["kmeans", "svm", "scaling", "pca"],
    "quiz": {
        "q": "在 50 维、只有 200 个样本的数据上用 kNN，最可能遇到的问题是？",
        "options": [
            "计算太慢，但精度不受影响",
            "所有点彼此几乎等距，「最近邻」失去区分度，精度显著下降",
            "模型参数太多导致过拟合",
            "K 必须是奇数才能收敛",
        ],
        "answer": 1,
        "why": "这是维度灾难的直接后果：最近邻距离趋于空间尺度，邻居与随机点无异。"
               "解法是降维（PCA/特征选择）、改用非距离模型，或换度量（马氏/余弦）。"
               "kNN 没有「参数太多」的问题，因为它压根不训练。",
    },
}
