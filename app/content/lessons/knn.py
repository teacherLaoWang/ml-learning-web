"""K 近邻教案。"""

LESSON = {
    "name": "K 近邻",
    "tagline": "它不训练，只把整本通讯录背下来当模型；预测时拦住离你最近的 K 个邻居问一句「你们怎么说」，然后听票。",
    "story": (
        "画面是一个从不复习、只把整本电话簿背下来的学生。1951 年 Fix 与 Hodges 给出最近邻分类的理论基础，"
        "1967 年 Cover 与 Hart 证明了一件惊人的事：样本量趋于无穷时，1-近邻的错误率不超过贝叶斯最优"
        "错误率的两倍——一个「什么都不学」的算法居然有这种保证。代价是它把计算全压到了预测那一刻，"
        "每次有人来问都得从头翻一遍簿子。这也是它今天仍活在推荐召回、异常检测、"
        "向量检索（kNN + 近似最近邻索引）里的原因。"
    ),
    "formula": {
        "text": "d(x, xᵢ) = (Σ|xⱼ − xᵢⱼ|^p)^(1/p)　　ŷ = majority{yᵢ : i ∈ N_K(x)}　　wᵢ = 1/d(x, xᵢ) 时 ŷ = argmax_c Σ_{i∈N_K, yᵢ=c} wᵢ",
        "latex": r"\begin{aligned}&& d_p(\mathbf{x},\mathbf{x}_i) &= \left(\sum_{j}\left|x_j - x_{ij}\right|^{p}\right)^{1/p}\\ && \hat{y} &= \operatorname{majority}\left\{y_i : i \in N_K(\mathbf{x})\right\}\\ && w_i = 1/d(\mathbf{x},\mathbf{x}_i) \;&\Rightarrow\; \hat{y} = \operatorname*{arg\,max}_{c}\sum_{i\in N_K,\,y_i=c} w_i\end{aligned}",
        "vars": [
            {"sym": "d", "zh": "距离：p=2 欧氏、p=1 曼哈顿、p→∞ 切比雪夫"},
            {"sym": "N_K(x)", "zh": "离 x 最近的 K 个训练样本集合"},
            {"sym": "K", "zh": "邻居数：唯一的「模型复杂度」旋钮，K 越小越灵活"},
            {"sym": "wᵢ", "zh": "距离加权时的权重，让近邻说话更大声"},
        ],
    },
    "intuition": [
        "kNN 没有「训练」这个阶段，它把训练数据本身当成模型：那张点阵图就是它全部的记性。"
        "所以它的偏差和方差不来自任何参数假设，而完全来自 K。K=1 时，决策边界会绕着每一个噪声点打转，"
        "边界碎成拼图（方差爆炸）；K=n 时，所有点被算作同一伙，模型退化成多数类（偏差爆炸）。"
        "误差随 K 走出的那条 U 形曲线，就是本页最核心的图。拖「邻居数 K」滑块，"
        "眼睛同时盯「误差 vs K」那张折线图：里面有条竖虚线标着你此刻站在 U 形的哪一段上。",
        "选哪种距离，就是在选「近」这个字的意思。想象在城里问路：飞过屋顶走直线是欧氏，"
        "只能沿着街道拐角走是曼哈顿，只挑最吃亏的那个方向算账是切比雪夫。"
        "代入 x=(1, 9) 与 x'=(2, 1)：欧氏距离 $\\sqrt{1+64}=8.06$，曼哈顿 1+8=9，切比雪夫 max(1,8)=8。"
        "同一对点，三把尺子量出三个数。若第二个特征是「星期几」这种本身没有大小的列，"
        "它就一把抢走了邻居的选取权。所以 kNN 对特征缩放极其敏感：必须先标准化或归一化，"
        "再考虑特征加权，或者改用马氏距离。在这页把「距离阶数 p」那根滑块左右拖，"
        "看「决策区域」那张图的台阶边怎么从圆滑变成沿街道的方折。",
        "维度灾难这件事是可以算的：同样多的人，先放进一条长街、再放进一间大厅、再放进一座体育馆，"
        "人没变少，彼此却越来越远，因为空间膨胀得比人快。形式化一点：单位超立方体内随机取点，"
        "最近邻距离的期望随维度 d 上升而趋近 1，而最远-最近距离的相对差 (max−min)/min 趋近 0 —— "
        "所有点都「一样远」，「邻居」这个概念失去意义。经验上超过 10~20 维就应先降维，"
        "PCA 和 t-SNE 那两页正好能对照着看。",
        "在这页的 3D 视图里，高度就是投票结果，所以边界天生是台阶：一边一块高台，"
        "中间那道棱就是决策边界。把 K 从 1 调到 41，你会看到台阶边缘从锯齿磨成缓坡，"
        "贴边的那些碎方块一块块并成大块——这就是方差被平均掉的几何图像。"
        "旁边的「查询点的邻居投票」会跟着变长：K 调大就是在末尾追加一排新柱子，"
        "老柱子的高度不动，但它们占总票的份额被摊薄了——平均掉的就是这些新来的声音。",
    ],
    "derivation": [
        {
            "title": "① 找一个点的预测",
            "body": "想象你在陌生的城里拦路问方向，只拦身边最近的那几个人，然后听多数。"
                    "训练集只有五个点：(0,0)→A、(0,1)→A、(1,0)→B、(1,1)→B、(2,2)→B。"
                    "查询点 x=(0.9, 0.1)，K=3。它到五个点的欧氏距离分别 0.906、1.273、0.141、0.906、2.195 → "
                    "最近三个是 B(0.141)、A(0.906)、B(0.906) → 多数 B，预测 B。"
                    "改成距离加权 w=1/d：B 得 1/0.141 + 1/0.906 = 7.09 + 1.10 = 8.19，"
                    "A 得 1/0.906 = 1.10 → B 压倒性胜出——因为最近的邻居投了决定性一票。"
                    "这道题就摊在「查询点的邻居投票」那排柱子上：柱高就是每个人的话语权。"
                    "把「距离加权」那根滑块从最左拨到最右，远邻的柱子立刻塌下去、近邻的拔起来，"
                    "一眼就能看出是谁把票定死的。",
            "formula": "预测 = argmax_c Σ_{i∈N_K, yᵢ=c} 1/d(x,xᵢ)",
        },
        {
            "title": "② 偏差-方差的解析图像",
            "body": "问一个人指路，很容易被那个人的偏见带跑；问所有人，只会得到同一句废话。"
                    "K=1 时，模型的决策函数在每个训练点附近剧烈跳动，换一批训练数据边界就完全变形 → "
                    "高方差、低偏差。K→n 时决策函数变成常数（永远是多数类）→ 零方差、最高偏差。"
                    "对 K 个邻居取平均，就像给照片开磨皮：独立噪声的方差按 1/K 缩小，"
                    "但同时把邻域里真实的类别差异也一起抹平了，于是偏差上升。"
                    "最优 K 通常落在「刚开始明显磨过头」的位置，用交叉验证挑它。"
                    "在这页慢慢推「邻居数 K」，两只眼睛分开用：看「决策区域」那张图的台阶被磨平的那一瞬，"
                    "再看「误差 vs K」里那条线什么时候开始抬头——先变好、后变坏，中间那个弯就是答案。",
            "formula": "Err(K) ≈ Bias²(K) + Variance(K)/K + 不可约噪声",
        },
        {
            "title": "③ 维度的诅咒算一次",
            "body": "同样多的人，先站满一条长街，再塞进一间大厅，最后丢进一座体育馆：人一个没少，"
                    "彼此却越来越远，因为空间膨胀得比人快。在 [0,1]^d 中随机撒 n 个点，"
                    "用第 n 维均匀化的近似：最近邻距离 ≈ $\\big(1/(n+1)\\big)^{1/d}$。"
                    "代进去看：d=2、n=100 → 0.099；d=10 → (1/101)^0.1 = 0.63；d=50 → 0.912。"
                    "维度升到 50，「最近」的邻居距离已经接近整个空间的尺度，邻居不邻居都没有区分度了。"
                    "要维持同样的相对距离，样本量必须随维度指数增长。"
                    "本页只有两根特征轴，看不见这场崩塌；把「样本数」拖到最右，能看到的只是点变密、"
                    "台阶边缘长出更多小碎块——那是低维里加数据的正常收益，跟高维的「大家一样远」不是一回事。",
            "formula": "d_min ≈ (1/(n+1))^(1/d) → 1（当 d→∞）",
        },
        {
            "title": "④ 复杂度与加速",
            "body": "暴力检索就是每次有人问路，都把整本电话簿从头翻到尾：每个查询点要和全部 n 个样本比一次，"
                    "O(n·d)，预测 10 万条高维向量代价巨大。想快，就得事先把书按部首分格，"
                    "问的时候只翻其中几格。近似最近邻（ANN，Approximate Nearest Neighbor）"
                    "就是这么干的：KD-Tree 按维交替切空间，低维很有效，d>15 就退化回线性；"
                    "再往上是 Ball Tree，或者 HNSW/LSH/IVF-PQ 这类图与哈希方法，"
                    "把检索降到近似 O(log n)，用一点召回率换巨大的速度提升。这也是「向量数据库」的核心技术。"
                    "本页是反过来的实验：把「样本数」拖到最右，点变密了，可每次改参数系统都要重算一遍全场——"
                    "你会立刻明白，kNN 的账全记在预测那一刻。",
            "formula": "暴力 O(n·d)　vs　KD-Tree 平均 O(d·log n)",
        },
    ],
    "terms": [
        {"term": "惰性学习", "full": "Lazy / Instance-based Learning", "explain": "建模那一步干脆省掉：数据本身就是模型，所有计算都推迟到有人来问的那一刻。"},
        {"term": "欧氏距离", "full": "L2 Norm", "explain": "飞过屋顶量直线的尺子，也就是 p=2 的 Minkowski 距离；特征量纲不同时它会偏心。"},
        {"term": "曼哈顿距离", "full": "L1 / City-block", "explain": "只能沿街道走的算法：各维绝对差加起来。对单维离群值更稳健，高维下也还留着区分度。"},
        {"term": "Minkowski", "full": "Minkowski Distance", "explain": "一整族距离，靠指数 p 调软硬：p 越大越只挑最吃亏那一维，p→∞ 就是切比雪夫。"},
        {"term": "维度灾难", "full": "Curse of Dimensionality", "explain": "空间随维度膨胀得比数据快，点与点趋于等距，一切靠距离说话的方法同时失效。"},
        {"term": "KD-Tree", "full": "k-dimensional Tree", "explain": "把空间按维度一刀刀交替切开的索引树，找邻居只翻几个格子；维度一高就退化成从头扫。"},
        {"term": "ANN", "full": "Approximate Nearest Neighbor", "explain": "近似最近邻：不再保证找到真正最近的那个，只求快——HNSW、LSH、IVF-PQ 都属此类。"},
        {"term": "曲率参数 K", "full": "K in kNN", "explain": "kNN 唯一那根容量旋钮：K 小则边界贴着噪声走，K 大则一切被抹平。它和核方法的带宽、树的深度是同一件事的三种写法。"},
    ],
    "presets": [
        {"id": "noisy", "label": "K=1（记住噪声）", "params": {"k": 1, "weighted": 0}},
        {"id": "smooth", "label": "K=21（边界平滑）", "params": {"k": 21, "weighted": 1}},
        {"id": "city", "label": "曼哈顿距离 + 加权", "params": {"k": 9, "p": 1, "weighted": 1}},
    ],
    "pitfalls": [
        "现象：加了一个新特征，准确率反而暴跌 → 为什么：新特征没缩放，量纲大的那一维直接抢走了「谁离得近」"
        "的定义权，邻居全按它选。对策：所有特征统一标准化，或对特征加权；也可以拿树模型、朴素贝叶斯这类"
        "不做距离运算的模型来交叉对照。",
        "现象：类别不平衡时少数类几乎预测不出来 → 为什么：多数决天然偏向人多的一边，少数类被票数埋住。"
        "对策：开距离加权、把 K 调小一点，并且按类分别看召回率，别只盯总准确率。",
        "现象：训练集变大后预测越来越慢 → 为什么：kNN 的推理成本随样本数线性增长，要翻的簿子变厚了。"
        "对策：先降维或抽样，或改用 ANN 索引；线上服务通常换成「K-Means 压缩原型 + kNN」。",
        "现象：用交叉验证挑 K 时觉得反正没参数可学，就把缩放一步做到全量数据上 → 为什么：只要 scaler "
        "碰过验证折，它就已经偷看过答案，这是数据泄漏。对策：缩放只在训练折上 fit，验证折只 transform。",
        "现象：K 设成偶数，二分类偶尔平票 → 为什么：argmax 的结果改成由实现里的排列顺序说了算，"
        "答案会随机漂。对策：二分类用奇数 K，或者开距离权重，让平票几乎不可能出现。",
    ],
    "seeAlso": ["kmeans", "svm", "scaling", "pca"],
    "quiz": {
        "q": "在 50 维、只有 200 个样本的数据上用 kNN，最可能遇到的问题是？",
        "options": [
            "计算太慢，但精度不受影响",
            "所有点彼此几乎等距，「最近邻」失去区分度，精度显著下降",
            "模型参数太多导致过拟合",
            "K 必须是奇数才能收敛",
        ],
        "answer": 1,
        "why": "这是维度灾难的直接后果：最近邻距离趋于空间尺度，邻居与随机点无异。"
               "解法是降维（PCA/特征选择）、改用非距离模型，或换度量（马氏/余弦）。"
               "kNN 没有「参数太多」的问题，因为它压根不训练。",
    },
}
