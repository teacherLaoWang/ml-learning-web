"""多层感知机与反向传播教案。

本页数字口径与本站前身 `nn-forward-backprop.html` 完全一致：
x1=1.0, x2=0.5, y=1.0, w11=0.8, w21=0.6, w12=0.4, w22=0.9, v1=0.7, v2=0.5, b1=0.3, b2=-0.2, b3=0.1
（约定 w_ij = 第 i 个输入 → 第 j 个隐藏神经元），下面所有算例都基于这组默认参数。
"""

LESSON = {
    "name": "多层感知机与反向传播",
    "tagline": "堆几层「加权求和 + 非线性」，再用链式法则把误差原路退回每个权重。",
    "story": (
        "1943 年 McCulloch 与 Pitts 给出神经元的形式模型，1958 年 Rosenblatt 的感知机第一次能学；"
        "1969 年 Minsky 与 Papert 指出单层感知机连异或（XOR）都学不会，神经网络进入寒冬。"
        "1974 年 Kelso、1986 年 Rumelhart/Hinton/Williams 把多层网络的链式求导法则讲清楚（反向传播，"
        "Backpropagation，BP），2012 年 AlexNet 在 GPU 上把它跑深，才有了今天的深度学习。"
        "本页用的就是最小可完整手算的多层网络：2 个输入 → H 个隐藏神经元 → 1 个输出，一共 3H+3 个待学参数。"
    ),
    "formula": {
        "text": "前向：zⱼ = Σᵢ wᵢⱼxᵢ + bⱼ，hⱼ = σ(zⱼ)，z = Σⱼ vⱼhⱼ + b₃，o = σ(z)　　损失：L = (o − y)²",
        "vars": [
            {"sym": "xᵢ", "zh": "第 i 个输入（本页 i = 1, 2）"},
            {"sym": "wᵢⱼ", "zh": "第 i 个输入 → 第 j 个隐藏神经元的权重（约定：下标前者是来源，后者是目标）"},
            {"sym": "zⱼ / z", "zh": "加权和（pre-activation），未过激活函数的原始分数"},
            {"sym": "hⱼ", "zh": "隐藏神经元的激活输出 = σ(zⱼ)"},
            {"sym": "vⱼ", "zh": "第 j 个隐藏神经元 → 输出神经元的权重"},
            {"sym": "bⱼ / b₃", "zh": "偏置：把激活阈值整体挪动的可学常数"},
            {"sym": "σ", "zh": "Sigmoid 激活 σ(t)=1/(1+e⁻ᵗ)，值域 (0,1)，导数 σ′=σ(1−σ)"},
            {"sym": "o / y", "zh": "网络输出（预测概率）/ 目标标签"},
        ],
    },
    "intuition": [
        "前向传播（Forward Propagation, FP）就是「按顺序算算术」：先把每个隐藏神经元的加权和与激活算出来，"
        "再把这些结果当作输出层的输入。没有循环、没有回头，一层一层往前推。"
        "本页默认参数下：z₁ = 0.8×1.0 + 0.6×0.5 + 0.3 = 1.40 → h₁ = σ(1.40) = 0.802；"
        "z₂ = 0.4×1.0 + 0.9×0.5 − 0.2 = 0.65 → h₂ = σ(0.65) = 0.657；"
        "z = 0.7×0.802 + 0.5×0.657 + 0.1 = 0.990 → o = σ(0.990) = 0.729。目标 y = 1.0，"
        "所以损失 L = (0.729 − 1.0)² = 0.0734 —— 网络「已经挺像了但还不够自信」。",
        "反向传播（BP）解决的是另一个问题：这 0.0734 的错误，9 个参数各要背多少锅？"
        "只有一个指标（损失），却有 9 个旋钮，你必须知道「每个旋钮对结果的影响有多大」，才能各自调整一点点。"
        "这就是梯度 ∂L/∂w。因为网络是复合函数，影响只能顺着连线一层层传回去 —— 链式法则不是数学游戏，"
        "而是「责任分配」的唯一正确算法。",
        "把每个神经元的责任写成误差项 δ（希腊字母 delta）：δ = ∂L/∂z。先算输出层："
        "∂L/∂o = 2(o−y) = 2×(0.729−1.0) = −0.542；σ′(z) = o(1−o) = 0.729×0.271 = 0.198；"
        "于是 δ₃ = −0.542×0.198 = −0.107。再往回一层："
        "δ₁ = δ₃ × v₁ × σ′(z₁) = −0.107 × 0.7 × (0.802×0.198 = 0.159) = −0.0119。"
        "注意 δ₁ 比 δ₃ 小了近 10 倍：误差每往回传一层就被乘上一个 ≤0.25 的 σ′ 因子 —— "
        "这正是深层网络「梯度消失」的算术根源，也是 Sigmoid 后来被 ReLU 取代的原因。",
        "有了 δ 就能一口气写出所有梯度：∂L/∂vⱼ = δ₃hⱼ，∂L/∂wᵢⱼ = δⱼxᵢ，∂L/∂b = δ。"
        "代入数字：∂L/∂v₁ = −0.107×0.802 = −0.0858；∂L/∂w₁₁ = −0.0119×1.0 = −0.0119。"
        "取 η=0.5 更新：v₁ ← 0.7 − 0.5×(−0.0859) = 0.743（更信任 h₁），"
        "w₁₁ ← 0.8 + 0.0059（把 x₁ 说得更大声一点）。每一步都很小，但每步都朝着损失下降的方向 —— "
        "几万步之后，网络就把 XOR 这种「没有线性可分结构」的问题学会了。",
        "为什么需要隐藏层？单层「加权求和 + Sigmoid」只能画出一条直线边界（就是逻辑回归那页的斜坡），"
        "XOR 需要两条线，即两个隐藏神经元各画一条、输出层再做「与」的逻辑。"
        "把隐藏神经元数量从 1 加到 8，你会在 3D 的「输出 logit 曲面」上看到曲面从一块平板"
        "逐渐长出沟壑、最后能贴合同心圆或 XOR 的四个角 —— 这就是通用逼近定理（Universal Approximation "
        "Theorem）的可视化：单个隐藏层、足够多的神经元，可以任意精度逼近任何连续函数。",
    ],
    "derivation": [
        {
            "title": "① 前向：把 9 个参数和 2 个输入算成一个数",
            "body": "本页默认值分四步算，你可以拿计算器逐步核对："
                    "(1) z₁ = w₁₁x₁ + w₂₁x₂ + b₁ = 0.8(1.0) + 0.6(0.5) + 0.3 = 1.40，h₁ = 1/(1+e⁻¹·⁴) = 0.802；"
                    "(2) z₂ = 0.4(1.0) + 0.9(0.5) − 0.2 = 0.65，h₂ = 0.657；"
                    "(3) z = v₁h₁ + v₂h₂ + b₃ = 0.7(0.802) + 0.5(0.657) + 0.1 = 0.990，o = 0.729；"
                    "(4) L = (0.729 − 1.0)² = 0.0734。整个计算图是 2→2→1 的有向无环图，"
                    "每个节点只做一次加法一次非线性。",
            "formula": "L = (σ(v·σ(Wx + b₁) + b₃) − y)²",
        },
        {
            "title": "② 链式法则：单个权重为什么要乘三项",
            "body": "以 w₁₁ 为例，它影响 L 的路径只有一条：w₁₁ → z₁ → h₁ → z → o → L。"
                    "于是 ∂L/∂w₁₁ = ∂L/∂o · ∂o/∂z · ∂z/∂h₁ · ∂h₁/∂z₁ · ∂z₁/∂w₁₁ "
                    "= [2(o−y)] × [o(1−o)] × [v₁] × [h₁(1−h₁)] × [x₁] "
                    "= (−0.542) × (0.198) × (0.7) × (0.159) × (1.0) = −0.0119。"
                    "把前两项与 σ′(z₁) 合并就是 δ₁ = −0.0119，因此式子简化成 ∂L/∂w₁₁ = δ₁·x₁。"
                    "反向传播的「高效」正在于此：先算 δ（每层一次），再乘上游的值（局部导数），"
                    "整个网络所有梯度只需两次遍历（一遍前向、一遍反向），复杂度 O(参数量)，"
                    "而如果对每个权重单独做数值差分，代价是 O(参数量²) 次前向。",
            "formula": "∂L/∂w₁₁ = 2(o−y)·σ′(z)·v₁·σ′(z₁)·x₁ = δ₁·x₁",
        },
        {
            "title": "③ 为什么用 MSE 配 Sigmoid 会慢（以及和交叉熵的差别）",
            "body": "用 MSE 时梯度带着 σ′(z) = o(1−o) 这个因子。若 o 已经= 0.98 而 y = 0，"
                    "误差很大（0.98）但 o(1−o) = 0.0196 极小 → 梯度只有 2(0.98)(0.0196) = 0.038，学得很慢；"
                    "这很反直觉：错得越离谱，越不该慢。改用交叉熵 L = −[y·ln o + (1−y)·ln(1−o)] 时，"
                    "∂L/∂z 恰好化简成 (o − y)（Sigmoid 的导数被对数消掉了），"
                    "于是同样情形梯度是 0.98 —— 大错配大梯度。本页默认用 MSE 是为了让「平方」直观可算，"
                    "把损失换成交叉熵时你会在损失曲线上看到明显不同的下降速度。",
            "formula": "MSE: ∂L/∂z = 2(o−y)·o(1−o)　　CE: ∂L/∂z = (o − y)",
        },
        {
            "title": "④ 激活函数怎么选：看饱和与死区",
            "body": "Sigmoid：输出 (0,1)、导数最大 0.25，深 stacked 时每层至少缩小 4 倍 → 梯度消失；"
                    "且输出不为 0 中心，会让下一层加权和中带恒定偏移。tanh：值域 (−1,1)、零中心、导数最大 1，"
                    "浅层隐藏单元通常比 Sigmoid 快；但两端同样饱和。ReLU：max(0,t)，正区间导数恒为 1（不消失），"
                    "计算便宜，负区间完全为 0 → 学习率太大时可能「死掉」（永远输出 0，梯度永远 0）。"
                    "在本页把「输入幅度（饱和压力）」调到 6，Sigmoid/tanh 的曲线会立刻躺平，"
                    "而 ReLU 依然线性下降 —— 这是理解为什么现代网络以 ReLU 家族为主的最快方式。",
            "formula": "σ′=σ(1−σ) ≤ 0.25；tanh′=1−tanh² ≤ 1；ReLU′∈{0,1}",
        },
        {
            "title": "⑤ 权重空间切片：为什么「碗」其实很崎岖",
            "body": "只有 3 个参数时损失曲面是个漂亮的碗（见线性回归页）。9 个参数时我们无法直接画，"
                    "但可以沿两个选定方向切开这个 9 维曲面，看它的 2 维切片。你会看到："
                    "局部极小、鞍点、狭长的低损失沟槽（沟里梯度小、坡外梯度大）。"
                    "这解释了三件事：为什么要动量/Adam（沿沟方向累积速度）、为什么要学习率衰减"
                    "（越接近沟底越要小步）、为什么随机初始化要打破对称（若所有 w 相同，"
                    "隐藏神经元梯度完全相同，永远同步更新，等效只有一个神经元）。",
            "formula": "切片：L(w₀ + αu₁ + α₂u₂) —— 固定两个方向，扫出二维高度图",
        },
    ],
    "terms": [
        {"term": "FP", "full": "Forward Propagation", "explain": "前向传播：按层算加权和与激活，得到预测与损失。"},
        {"term": "BP", "full": "Backpropagation", "explain": "反向传播：用链式法则从输出往回算每个参数的梯度，两次遍历完成全部梯度。"},
        {"term": "MLP", "full": "Multi-Layer Perceptron", "explain": "多层感知机：全连接 + 非线性激活的前馈网络，本页的最小版本。"},
        {"term": "δ（误差项）", "full": "local gradient / error signal", "explain": "δ = ∂L/∂z，某神经元对其加权和的敏感度；梯度 = δ × 上游输入值。"},
        {"term": "梯度消失", "full": "Vanishing Gradient", "explain": "每回传一层乘以 σ′≤0.25（或 tanh′<1），层数深时梯度指数衰减，前层几乎不更新。"},
        {"term": "SGD", "full": "Stochastic Gradient Descent", "explain": "随机梯度下降：逐样本（或小批）更新，w ← w − η·∇L。"},
        {"term": "学习率 η", "full": "Learning Rate", "explain": "每步沿梯度反方向走多远；太大发散、太小停滞，是最重要的超参数。"},
        {"term": "饱和", "full": "Saturation", "explain": "Sigmoid/tanh 输入绝对值大时输出贴边、导数趋 0 的状态；由输入幅度过大引起。"},
        {"term": "ReLU 死区", "full": "Dying ReLU", "explain": "ReLU 单元持续输出负值 → 梯度恒 0 → 永久失活；Leaky ReLU/GELU 可缓解。"},
        {"term": "对称性破缺", "full": "Symmetry Breaking", "explain": "随机初始化的真正目的：让不同隐藏单元有不同的更新轨迹。"},
        {"term": "通用逼近定理", "full": "Universal Approximation Theorem", "explain": "单隐藏层、足够多神经元、非线性激活可任意精度逼近紧集上的连续函数；不保证学得快、也不保证层数少。"},
        {"term": "鞍点", "full": "Saddle Point", "explain": "梯度为 0 但不是极小（某些方向上、某些方向下）；高维优化里比局部极小更常见。"},
    ],
    "presets": [
        {"id": "xor", "label": "XOR：必须靠隐藏层", "params": {"data": 1, "hidden": 2, "act": 1, "lr": 1.2}},
        {"id": "circles", "label": "同心圆 + 8 神经元", "params": {"data": 2, "hidden": 8, "act": 1, "lr": 0.8}},
        {"id": "vanish", "label": "饱和与梯度消失（演示）", "params": {"data": 0, "hidden": 4, "act": 0, "lr": 0.3, "sat": 6.0}},
    ],
    "pitfalls": [
        "现象：损失前几十步几乎不动，然后突然下降 → Sigmoid/tanh 饱和导致梯度极小的「平台期」。"
        "对策：标准化输入（本页把「输入幅度」调回 1 左右即可复现/消除）、改用 tanh/ReLU 家族、加大初期学习率或用动量。",
        "现象：所有隐藏神经元学到的东西一模一样 → 初始化全相同（或全 0）。"
        "对策：对称的小随机数（如 Xavier/He 初始化）；偏置可以设 0，权重不行。",
        "现象：损失曲线剧烈震荡甚至 NaN → 学习率太大，或梯度爆炸。对策：缩小 η、梯度裁剪（clip-norm）、加 BatchNorm、用 warmup。",
        "现象：训练集 100% 准确、测试集 60% → 网络容量远超数据，把噪声记住了。"
        "对策：减少神经元/层数、加权重衰减（L2）、dropout、早停，或者老实收集更多数据。",
        "现象：把隐藏神经元数量一直加大，结果反而不如小的 → 深/宽不是万能，梯度更难协调，且小数据集容易过拟合。"
        "对策：先用能跑通的最小结构，再按验证集曲线一层层加。",
        "现象：调试时怀疑「梯度写错了」→ 这是反向传播最常见的 bug。"
        "对策：数值差分核对：[L(w+ε) − L(w−ε)]/(2ε) 应与解析梯度一致（本站实现里已用 pytest 断言误差 < 1e-6）。",
    ],
    "seeAlso": ["cnn", "optim", "normalization", "logreg", "biasvariance"],
    "quiz": {
        "q": "用本页默认参数（2-2-1，Sigmoid，MSE），为什么 δ₁（隐藏神经元 1 的误差项）比 δ₃（输出层）小得多？",
        "options": [
            "因为 w₁₁ 太小",
            "因为误差回传时要乘以 v₁ 与 σ′(z₁)，而 Sigmoid 的导数最大只有 0.25，每回传一层就至少缩小 4 倍",
            "因为隐藏层没有参与损失计算",
            "因为学习率 η 太小",
        ],
        "answer": 1,
        "why": "δ₁ = δ₃ · v₁ · σ′(z₁)。代入数字：−0.107 × 0.7 × 0.159 = −0.0119，约小 9 倍。"
               "这个连乘 ≤0.25 因子的结构就是梯度消失；层数一深，前几层几乎收不到信号。"
               "与学习率无关（η 只影响更新步长，不影响梯度本身），也与「隐藏层是否参与损失」无关。",
    },
}
