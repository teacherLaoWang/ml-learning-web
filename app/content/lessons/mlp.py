"""多层感知机与反向传播教案。

本页数字口径与本站前身 `nn-forward-backprop.html` 完全一致：
x1=1.0, x2=0.5, y=1.0, w11=0.8, w21=0.6, w12=0.4, w22=0.9, v1=0.7, v2=0.5, b1=0.3, b2=-0.2, b3=0.1
（约定 w_ij = 第 i 个输入 → 第 j 个隐藏神经元），下面所有算例都基于这组默认参数。
"""

LESSON = {
    "name": "多层感知机与反向传播",
    "tagline": "一层层「加权求和 + 拧一把弯」叠上去；学的时候把误差沿原路退回，挨个敲每个权重的门。",
    "story": (
        "这段历史走过两个冬天，最后才加速。"
        "1943 年 McCulloch 与 Pitts 给出神经元的形式模型，1958 年 Rosenblatt 的感知机第一次能学；"
        "1969 年 Minsky 与 Papert 指出单层感知机连异或（XOR，输入不同才亮的那种开关）都学不会，"
        "神经网络进入寒冬。1974 年 Kelso、1986 年 Rumelhart/Hinton/Williams 把多层网络的链式求导法则讲清楚"
        "（反向传播，Backpropagation，BP），2012 年 AlexNet 在 GPU 上把它跑深，才有了今天的深度学习。"
        "本页用的就是最小可完整手算的多层网络：2 个输入 → H 个隐藏神经元 → 1 个输出，"
        "一共 3H+3 个待学参数。"
    ),
    "formula": {
        "text": "前向：zⱼ = Σᵢ wᵢⱼxᵢ + bⱼ，hⱼ = σ(zⱼ)，z = Σⱼ vⱼhⱼ + b₃，o = σ(z)　　损失：L = (o − y)²",
        "latex": r"\begin{aligned}&& \text{前向： } z_j &= \sum_{i}w_{ij}x_i + b_j, \qquad h_j = \sigma(z_j), \qquad z = \sum_{j}v_jh_j + b_3, \qquad o = \sigma(z)\\ && \text{损失： } L &= (o-y)^2\end{aligned}",
        "vars": [
            {"sym": "xᵢ", "zh": "第 i 个输入（本页 i = 1, 2）"},
            {"sym": "wᵢⱼ", "zh": "第 i 个输入 → 第 j 个隐藏神经元的权重（约定：下标前者是来源，后者是目标）"},
            {"sym": "zⱼ / z", "zh": "加权和（pre-activation），未过激活函数的原始分数"},
            {"sym": "hⱼ", "zh": "隐藏神经元的激活输出 = σ(zⱼ)"},
            {"sym": "vⱼ", "zh": "第 j 个隐藏神经元 → 输出神经元的权重"},
            {"sym": "bⱼ / b₃", "zh": "偏置：把激活阈值整体挪动的可学常数"},
            {"sym": "σ", "zh": "Sigmoid 激活 σ(t)=1/(1+e⁻ᵗ)，值域 (0,1)，导数 σ′=σ(1−σ)"},
            {"sym": "o / y", "zh": "网络输出（预测概率）/ 目标标签"},
        ],
    },
    "intuition": [
        "前向传播（Forward Propagation, FP）就是依次传话。每个神经元只干三件事："
        "听上游几个人说话、按信任度加权加起来、再把和挤进一条 S 形斜坡，输出变成一个「信几分」的数。"
        "没有循环、没有回头，一层一层往前推。本页默认参数下："
        "z₁ = 0.8×1.0 + 0.6×0.5 + 0.3 = 1.40 → h₁ = σ(1.40) = 0.802；"
        "z₂ = 0.4×1.0 + 0.9×0.5 − 0.2 = 0.65 → h₂ = σ(0.65) = 0.657；"
        "z = 0.7×0.802 + 0.5×0.657 + 0.1 = 0.990 → o = σ(0.990) = 0.729。目标 y = 1.0，"
        "所以损失 L = (0.729 − 1.0)² = 0.0734 —— 网络已经挺像了，但还不够自信。"
        "这条链子在「网络结构与参数」那张图上是摊开的：每条连线旁边写的就是那个权重数值，"
        "每个节点下面写的是它的平均激活。鼠标顺着箭头从左走到右，就等于把上面三行算式走一遍；"
        "拖动「隐藏神经元数」，中间那排节点当场增减，前向的加法链也跟着多一项、少一项。",
        "反向传播（BP）解决的是另一个问题：这 0.0734 的错误，9 个参数各要背多少锅？"
        "画面是一台有 9 个旋钮的老收音机，外面却只露出一个指示灯：它只告诉你现在有多吵，"
        "不告诉你该拧哪一个。你得先量出每个旋钮对结果的影响有多大，才能各自调一点点，"
        "这个「影响有多大」就是梯度 ∂L/∂w。因为网络是复合函数，影响只能顺着连线一层层退回 —— "
        "链式法则不是数学游戏，而是「责任分配」的唯一正确算法。"
        "训练跑起来之后去看「损失与准确率」那张曲线图：虚线是梯度范数 ‖∇L‖，"
        "它掉得多快，就是这些旋钮被一起推动的速度。",
        "把每个神经元该背的锅写成一个数 δ（希腊字母 delta，本站叫它误差项）：δ = ∂L/∂z，"
        "白话是「这个神经元的原始分数抖一下，损失跟着抖多少」。先算输出层那笔总账："
        "∂L/∂o = 2(o−y) = 2×(0.729−1.0) = −0.542；Sigmoid 在这一点的坡度 σ′(z) = o(1−o) = "
        "0.729×0.271 = 0.198；于是 δ₃ = −0.542×0.198 = −0.107。再往回问一层："
        "δ₁ = δ₃ × v₁ × σ′(z₁) = −0.107 × 0.7 × (0.802×0.198 = 0.159) = −0.0119。"
        "回声一层比一层轻：δ₁ 比 δ₃ 小了近 10 倍，因为每退一层都要再乘上一个坡度因子，"
        "而 $\\sigma' \\le 0.25$。这正是深层网络「梯度消失」的算术根源，也是 Sigmoid 后来被 ReLU 取代的原因。"
        "想亲眼看这层回声：把「激活函数」留在 Sigmoid、把「输入幅度」滑块推大，"
        "「损失与准确率」里那条梯度虚线会贴到几乎看不见。",
        "有了 δ，写梯度就退化成把两个数相乘：∂L/∂vⱼ = δ₃hⱼ，∂L/∂wᵢⱼ = δⱼxᵢ，∂L/∂b = δ。"
        "画面是调音台：推子该推多少 = 这条连线该负的责任 × 说话人当时的音量。代入数字："
        "∂L/∂v₁ = −0.107×0.802 = −0.0858；∂L/∂w₁₁ = −0.0119×1.0 = −0.0119。"
        "取 η=0.5 走一步：v₁ ← 0.7 − 0.5×(−0.0859) = 0.743，也就是更信任 h₁；"
        "w₁₁ ← 0.8 + 0.0059，把 x₁ 说得更大声一点。每步都只有千分位那么大，"
        "但每步都朝着损失下降的方向 —— 几万步之后，网络就把 XOR 这种「找不到一条直线能分开」的问题学会了。"
        "在本页把「学习率 η」推大，「网络结构与参数」图上连线旁边的数字每帧改口改得更猛，"
        "直到「输出 logit 曲面」上那条分界线开始左右晃 —— 那就是步子迈过了头。",
        "为什么非得有隐藏层？想象切一块蛋糕：单层「加权求和 + Sigmoid」只能落一刀直的，"
        "切口就是一条直线边界，也就是逻辑回归那页的斜坡。XOR 要落两刀："
        "两个隐藏神经元各切一条线，输出层再把两刀的结果做「与」。"
        "把「隐藏神经元数」从 1 加到 8，你会在 3D 的「输出 logit 曲面」上看到曲面从一块平板"
        "逐渐长出沟壑，最后能贴合同心圆或 XOR 的四个角 —— 这就是通用逼近定理"
        "（Universal Approximation Theorem）能被看见的样子：单个隐藏层、足够多的神经元，"
        "可以任意精度逼近任何连续函数。",
    ],
    "derivation": [
        {
            "title": "① 前向：把 9 个参数和 2 个输入算成一个数",
            "body": "这一步没有新思想，就是把数老老实实乘完，像依次解开三道锁。"
                    "本页默认值分四步算，你可以拿计算器逐步核对："
                    "(1) z₁ = w₁₁x₁ + w₂₁x₂ + b₁ = 0.8(1.0) + 0.6(0.5) + 0.3 = 1.40，"
                    "h₁ = 1/(1+e⁻¹·⁴) = 0.802；"
                    "(2) z₂ = 0.4(1.0) + 0.9(0.5) − 0.2 = 0.65，h₂ = 0.657；"
                    "(3) z = v₁h₁ + v₂h₂ + b₃ = 0.7(0.802) + 0.5(0.657) + 0.1 = 0.990，o = 0.729；"
                    "(4) L = (0.729 − 1.0)² = 0.0734。"
                    "整个计算图是 2→2→1 的有向无环图，每个节点只做一次加法、一次非线性："
                    "没有分支也没有回路，所以责任能沿一条通路一路乘到底。"
                    "对照「网络结构与参数」那张图从左往右走一遍：左边两个输入节点、中间一排隐藏节点、"
                    "最右一个输出节点，每根箭头上都标着自己的数，上面四步里每个符号都能在图上指到。",
            "formula": "L = (σ(v·σ(Wx + b₁) + b₃) − y)²",
        },
        {
            "title": "② 链式法则：单个权重为什么要乘三项",
            "body": "以 w₁₁ 为例。它想影响 L，只有一条路可走，像一排多米诺骨牌："
                    "w₁₁ → z₁ → h₁ → z → o → L。每推过一道门，作用就被那道门的坡度打一次折，"
                    "于是 ∂L/∂w₁₁ = ∂L/∂o · ∂o/∂z · ∂z/∂h₁ · ∂h₁/∂z₁ · ∂z₁/∂w₁₁ "
                    "= [2(o−y)] × [o(1−o)] × [v₁] × [h₁(1−h₁)] × [x₁] "
                    "= (−0.542) × (0.198) × (0.7) × (0.159) × (1.0) = −0.0119。"
                    "把前两项与 σ′(z₁) 合并就是 δ₁ = −0.0119，式子于是缩成一句：∂L/∂w₁₁ = δ₁·x₁。"
                    "反向传播的「省」正在这儿：先按层算出 δ，再乘上游的值（局部导数），"
                    "整个网络所有梯度只需两趟遍历，一趟前向、一趟反向，复杂度 O(参数量)。"
                    "要是偷懒，对每个权重单独做数值差分，代价是 O(参数量²) 次前向。"
                    "本页「权重空间切片」走的就是这两趟：拖动视角，看轨迹沿沟槽一级级往下滚，"
                    "每一格都对应这样一次连乘。",
            "formula": "∂L/∂w₁₁ = 2(o−y)·σ′(z)·v₁·σ′(z₁)·x₁ = δ₁·x₁",
        },
        {
            "title": "③ 为什么用 MSE 配 Sigmoid 会慢（以及和交叉熵的差别）",
            "body": "这是个会让人误以为「网络不想学」的坑：用 MSE 时，梯度尾巴上挂着 σ′(z) = o(1−o) 这个因子。"
                    "画面是一辆油门和喇叭共用一根线的车 —— 车越开越歪，喇叭本该越响，它却越来越哑。"
                    "算一遍就懂：若 o 已经= 0.98 而 y = 0，误差 0.98 大得离谱，"
                    "但 o(1−o) = 0.0196 小得可怜，梯度只有 2(0.98)(0.0196) = 0.038，学得很慢。"
                    "反直觉的地方就在这儿：错得越狠，越不该慢。"
                    "改用交叉熵 L = −[y·ln o + (1−y)·ln(1−o)] 时，Sigmoid 的导数被对数消掉了，"
                    "∂L/∂z 恰好化简成 (o − y)，于是同样情形梯度是 0.98 —— 大错配大梯度。"
                    "本页默认用 MSE，是为了让「平方」直观可算。想看到这只哑喇叭："
                    "把「激活函数」留在 Sigmoid，再把「输入幅度」滑块推大，"
                    "「损失与准确率」里那条 loss 曲线起步就压成一段平台，撑很久才掉下去；"
                    "再把「学习率 η」从小拖到大，平台会被缩短但开头始终是平的 —— "
                    "形状是 σ′ 决定的，不是 η 决定的。",
            "formula": "MSE: ∂L/∂z = 2(o−y)·o(1−o)　　CE: ∂L/∂z = (o − y)",
        },
        {
            "title": "④ 激活函数怎么选：看饱和与死区",
            "body": "三种激活函数就是三种不同形状的坡。Sigmoid：中间陡、两头躺平的 S 形坡，输出 (0,1)、"
                    "导数最大 0.25，网络一深，回声每层至少缩小 4 倍 → 梯度消失；"
                    "而且它的输出不为 0 中心，下一层的加权和里会带着一个恒定偏移，像整条队伍被推歪半步。"
                    "tanh：同一条坡拉得更陡，值域 (−1,1)、零中心、导数最大 1，"
                    "浅层隐藏单元通常比 Sigmoid 快，但两端照样躺平。"
                    "ReLU：max(0,t)，像一块直角挡板，正区间导数恒为 1（不消失）、算得便宜，"
                    "负区间完全为 0 → 学习率太大时可能「死掉」，永远输出 0，梯度也永远 0。"
                    "验证只需一步：在本页把「输入幅度」滑块调到 6，"
                    "「激活函数」切到 Sigmoid 或 tanh，看「损失与准确率」里那条 loss 曲线怎么趴着不动；"
                    "再切到 ReLU，同一位置的曲线立刻斜下去 —— 这就是现代网络偏爱 ReLU 家族最快的理由。",
            "formula": "σ′=σ(1−σ) ≤ 0.25；tanh′=1−tanh² ≤ 1；ReLU′∈{0,1}",
        },
        {
            "title": "⑤ 权重空间切片：为什么「碗」其实很崎岖",
            "body": "把高维地形想成一张摸不到的大地毯：你看不见全貌，只能用刀切下去，看切口的高度线。"
                    "只有 3 个参数时，这块地毯是个漂亮的碗，见线性回归那页；"
                    "9 个参数时已经画不出来了，但仍能沿两个选定方向切开这个 9 维曲面，看它的 2 维切片。"
                    "切出来的地形有三种：局部极小、鞍点（哪个方向都不算最糟，却也走不出去）、"
                    "以及狭长的低损失沟槽 —— 沟里坡很缓、坡外坡很陡。"
                    "这一张切片一次解释了三件事：为什么要动量或 Adam（顺沟攒速度）、"
                    "为什么要学习率衰减（越近沟底越要小步）、为什么随机初始化要打破对称"
                    "（若所有 w 相同，隐藏神经元梯度完全相同，永远同步更新，等效只有一个神经元）。"
                    "本页「权重空间切片」就是这样一个切口：拖动鼠标旋转它，看轨迹沿沟壁一级级往下滚；"
                    "标着「切片内最低点」的那个位置不一定是全局最优，这正是地形的脾气。",
            "formula": "切片：L(w₀ + αu₁ + α₂u₂) —— 固定两个方向，扫出二维高度图",
        },
    ],
    "terms": [
        {"term": "FP", "full": "Forward Propagation",
         "explain": "前向传播：一层层算加权和与激活，直到预测和损失落地；只往前走、不回头的那一趟。"},
        {"term": "BP", "full": "Backpropagation",
         "explain": "反向传播：用链式法则从输出往回算每个参数的梯度，全网络两趟遍历就算完，不必逐个权重去试探。"},
        {"term": "MLP", "full": "Multi-Layer Perceptron",
         "explain": "多层感知机：全连接 + 非线性激活的前馈网络，也就是本页那个可以完整手算的最小版本。"},
        {"term": "δ（误差项）", "full": "local gradient / error signal",
         "explain": "δ = ∂L/∂z：这个神经元的原始分数抖一下，损失跟着抖多少；梯度 = δ × 上游那个说话人的音量。"},
        {"term": "梯度消失", "full": "Vanishing Gradient",
         "explain": "每回传一层就被乘上 $\\sigma' \\le 0.25$（或 tanh′<1），层一深，信号像回声一样衰减到听不见，前层几乎不更新。"},
        {"term": "SGD", "full": "Stochastic Gradient Descent",
         "explain": "随机梯度下降：每次只看一个样本或一小批，就把旋钮轻轻推一格，w ← w − η·∇L。"},
        {"term": "学习率 η", "full": "Learning Rate",
         "explain": "每步沿梯度反方向走多远：太小像蜗牛爬坡，太大直接迈过山谷，是最重要的超参数。"},
        {"term": "饱和", "full": "Saturation",
         "explain": "Sigmoid/tanh 的输入绝对值一大，输出就贴到坡道尽头、导数趋 0：踩足油门车却不动。由输入幅度过大引起。"},
        {"term": "ReLU 死区", "full": "Dying ReLU",
         "explain": "ReLU 单元持续输出负值 → 梯度恒 0 → 永久失活，像个被焊死的开关；Leaky ReLU/GELU 给它留了条缝。"},
        {"term": "对称性破缺", "full": "Symmetry Breaking",
         "explain": "随机初始化的真正目的：让每个隐藏神经元从不同位置出发，从此走上各自的更新轨迹，不再合唱。"},
        {"term": "通用逼近定理", "full": "Universal Approximation Theorem",
         "explain": "单隐藏层、足够多神经元、非线性激活，能以任意精度逼近紧集上的连续函数；至于学得快不快、层数少不少，它不管。"},
        {"term": "鞍点", "full": "Saddle Point",
         "explain": "梯度为 0 却不是最低点：这个方向上坡、那个方向下坡，像山坳的口；高维优化里它比局部极小更常见。"},
    ],
    "presets": [
        {"id": "xor", "label": "XOR：必须靠隐藏层", "params": {"data": 1, "hidden": 2, "act": 1, "lr": 1.2}},
        {"id": "circles", "label": "同心圆 + 8 神经元", "params": {"data": 2, "hidden": 8, "act": 1, "lr": 0.8}},
        {"id": "vanish", "label": "饱和与梯度消失（演示）", "params": {"data": 0, "hidden": 4, "act": 0, "lr": 0.3, "sat": 6.0}},
    ],
    "pitfalls": [
        "现象：损失前几十步几乎不动，然后突然掉下去 → 为什么：Sigmoid/tanh 躺在饱和区里，"
        "梯度被压得几乎看不见，这就是「平台期」。对策：标准化输入 —— 本页把「输入幅度」调回 1 左右就能复现或消除；"
        "再换 tanh/ReLU 家族、加大初期学习率，或者用动量把车推出平地。",
        "现象：所有隐藏神经元学到的东西一模一样 → 为什么：初始化全相同（或者全 0），"
        "大家算出同一个数、也朝同一个方向更新，等于只有一根弦在响。"
        "对策：对称的小随机数，例如 Xavier/He 初始化；偏置可以设 0，权重不行。",
        "现象：损失曲线剧烈震荡，甚至冒出 NaN → 为什么：学习率太大，梯度一路爆炸。"
        "对策：缩小 η、加梯度裁剪（clip-norm）、上 BatchNorm、用 warmup 慢慢把步子放开来。",
        "现象：训练集 100% 准确、测试集只有 60% → 为什么：网络容量远超数据量，把噪声一起背了下来。"
        "对策：减少神经元或层数、加权重衰减（L2）、dropout、早停，或者老实去多收点数据。",
        "现象：隐藏神经元数量一直加大，结果反而不如小的 → 为什么：结构更宽更深，梯度更难协调，"
        "小数据集上还更容易过拟合。对策：先用能跑通的最小结构，再照验证集曲线一层一层加。",
        "现象：调试时怀疑「梯度写错了」→ 这是反向传播最常见的 bug。"
        "对策：数值差分核对，[L(w+ε) − L(w−ε)]/(2ε) 应与解析梯度一致"
        "（本站实现里已用 pytest 断言误差 < 1e-6）。",
    ],
    "seeAlso": ["cnn", "optim", "normalization", "logreg", "biasvariance"],
    "quiz": {
        "q": "用本页默认参数（2-2-1，Sigmoid，MSE），为什么 δ₁（隐藏神经元 1 的误差项）比 δ₃（输出层）小得多？",
        "options": [
            "因为 w₁₁ 太小",
            "因为误差回传时要乘以 v₁ 与 σ′(z₁)，而 Sigmoid 的导数最大只有 0.25，每回传一层就至少缩小 4 倍",
            "因为隐藏层没有参与损失计算",
            "因为学习率 η 太小",
        ],
        "answer": 1,
        "why": "δ₁ = δ₃ · v₁ · σ′(z₁)。代入数字：−0.107 × 0.7 × 0.159 = −0.0119，约小 9 倍。"
               "这个连乘 ≤0.25 因子的结构就是梯度消失；层数一深，前几层几乎收不到信号。"
               "与学习率无关（η 只影响更新步长，不影响梯度本身），也与「隐藏层是否参与损失」无关。",
    },
}
