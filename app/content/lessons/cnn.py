"""卷积神经网络教案。"""

LESSON = {
    "name": "卷积神经网络",
    "tagline": "把「权重共享 + 局部连接」写进结构里：用一个 3×3 的小窗口扫遍整张图。",
    "story": (
        "1959 年 Hubel 与 Wiesel 在猫的视觉皮层发现：某些神经元只对视野里特定朝向的边缘放电（简单细胞），"
        "另一些对更复杂的组合响应（复杂细胞）——这就是「局部感受野 + 层级抽象」的生物学原型。"
        "1980 年福岛邦彦的 Neocognitron 把它写成网络，1989 年 LeCun 用反向传播训练 LeNet-5 读手写邮编，"
        "2012 年 AlexNet 用 GPU + ReLU + Dropout 在 ImageNet 上把错误率砍下 10 个百分点，"
        "2015 年 ResNet 用残差连接把深度推到 152 层。今天「卷积」这个思想还活在视觉之外："
        "1D 卷积处理音频与序列，2D 处理图像，3D 处理视频与体数据。"
    ),
    "formula": {
        "text": "O = ⌊(I − K + 2P)/S⌋ + 1　　Yₒ = f(Σ K⁽ᵒ⁾ ⊛ X + bₒ)　　参数量 = K·K·Cᵢₙ·Cₒᵤₜ + Cₒᵤₜ",
        "vars": [
            {"sym": "I", "zh": "输入边长（本页程序化生成的 16×16 图案，I=16）"},
            {"sym": "K", "zh": "卷积核尺寸（kernel size），如 3 表示 3×3 的权重矩阵"},
            {"sym": "P", "zh": "零填充（padding）：边缘补几圈 0，用来保住分辨率"},
            {"sym": "S", "zh": "步长（stride）：窗口每次滑动几格"},
            {"sym": "O", "zh": "输出特征图（feature map）边长"},
            {"sym": "Cᵢₙ / Cₒᵤₜ", "zh": "输入/输出通道数（filters 个数 = Cₒᵤₜ）"},
            {"sym": "⊛", "zh": "卷积（实现上常是滑动窗口点积）；f 通常取 ReLU"},
            {"sym": "⊙ / 点积", "zh": "对应位置相乘再全部相加——一个神经元就是一次加权和"},
        ],
    },
    "intuition": [
        "先把图像看成一摞数字：灰度图是一张矩阵，值越大越亮。本站把它画成 3D 高度场（「输入「图像」= 高度场」那张图），"
        "山峰就是亮区，山谷就是暗区。卷积核是另一张更小的矩阵，比如竖直边缘检测器 "
        "K = [[−1,0,1],[−1,0,1],[−1,0,1]]。把它压在图像窗口上做对应位置相乘再求和："
        "窗口里 patch = [[1,1,7],[1,1,7],[1,1,7]] → 每行 (−1×1 + 0×1 + 1×7) = 6，三行合计 18 → 强响应，"
        "说明这里有一条「左暗右亮」的竖直边缘；换成水平的 patch 则算出 0。"
        "整张图滑一遍，就得到一张「每个位置有多像竖直边缘」的响应图。",
        "关键设计是权重共享：同一个 9 个数的核在图像每个位置复用一次。带来三个结果——"
        "参数从「密集连接」的天文数字降到 9 个；边缘在左上角还是在右下角都能被同一个核检出（平移等变性）；"
        "并且天然编码了「只看局部」的先验。代价是它假定「局部模式有意义」，"
        "对需要全局关系的问题（比如长距离依赖）就不是好先验——那正是注意力机制登场的原因。",
        "算一笔参数量账（用「各层参数量」柱状图对照）：输入 16×16、3 个 3×3 核，"
        "参数量 = 3×3×1×3 + 3 = 30。若改成全连接层把 256 维映到 256 维，需要 256×256 + 256 = 65792 个参数——"
        "相差 2000 多倍，而小图像上两者表达能力接近。「局部 + 共享」就是省参数的全部秘密。",
        "输出尺寸公式必须能手算（本页「输出边长 / 参数量」曲线会实时验证）："
        "I=16, K=3, P=1, S=1 → O = ⌊(16−3+2)/1⌋+1 = 16（尺寸不变，这就是 padding=1 的用意）；"
        "P=0 → O = 14（每层缩 2）；S=2, P=1 → O = ⌊15/2⌋+1 = 8（下采样一半）。"
        "核尺寸取奇数（1/3/5/7）是为了让窗口有正中心、padding 能对称。",
        "堆叠是抽象的来源：第一层响应边缘与颜色梯度，第二层的输入已经是「边缘强度图」，"
        "于是它能对「两条边缘以直角相遇」响应（角点），再往上出现纹理、部件、整体。"
        "感受野（Receptive Field, RF）随之扩大：两个 3×3 层（步长都是 1）叠加，"
        "RF = 1 + (3−1) + (3−1) = 5，等价于一个 5×5，但只用 9+9=18 个参数而不是 25 个，还多了一次非线性。"
        "「小核堆深」比「大核单层」更省、更强——这是 ResNet 之后所有现代骨干网的共识。",
    ],
    "derivation": [
        {
            "title": "① 一次卷积的完整算术",
            "body": "取 patch = [[1,1,7],[1,1,7],[1,1,7]]，核 K = [[−1,0,1],[−1,0,1],[−1,0,1]]，偏置 b = −2。"
                    "逐元素相乘：第一列 ×(−1) 得 (−1,−1,−1)，第二列 ×0 全 0，第三列 ×1 得 (7,7,7)；"
                    "全部相加 = 3×(−1) + 3×7 = −3 + 21 = 18；加偏置 18 − 2 = 16；过 ReLU：max(0,16) = 16 → 有边缘。"
                    "换一个「无边缘」的 patch = [[5,5,5],[5,5,5],[5,5,5]]：3×(−5) + 3×5 = 0，加偏置 −2，ReLU 后 = 0 → 无响应。"
                    "这就是「检测器」的含义：只在符合特定模式时输出正值。",
            "formula": "y = ReLU(ΣᵤΣᵥ x[u,v]·k[v,u] + b) = ReLU(18 − 2) = 16",
        },
        {
            "title": "② 输出尺寸公式怎么来的",
            "body": "把长为 I 的输入、宽 K 的窗口在补了 P 圈 0 的序列上滑动，窗口左上角可放的位置数即输出长度。"
                    "补零后总长 I + 2P，窗口能盖住的最右起点是 I + 2P − K，起点间隔为 S，"
                    "所以位置数 = ⌊(I + 2P − K)/S⌋ + 1。代数字：I=16,K=3,P=1,S=1 → 16；"
                    "I=16,K=3,P=0,S=1 → 14；I=16,K=3,P=1,S=2 → ⌊15/2⌋+1 = 8；"
                    "I=8,K=3,P=1,S=1 → 8；再叠一层 stride=1 → 仍 8。"
                    "本页把 stride 拉到 3、padding 归零时，能看到特征图「塌」成 6×6 甚至更小，"
                    "细节同时消失——这就是下采样过猛的代价。",
            "formula": "O = ⌊(I − K + 2P)/S⌋ + 1",
        },
        {
            "title": "③ 参数量与计算量",
            "body": "一个输出通道要看所有输入通道：参数量 = K·K·Cᵢₙ（每个输入通道一张小图）×Cₒᵤₜ + Cₒᵤₜ（每通道一个偏置）。"
                    "代入：3 个 3×3 核、Cᵢₙ=1 → 9×1×3 + 3 = 30；若 Cᵢₙ=8、Cₒᵤₜ=16、K=3 → 9×8×16 + 16 = 1168。"
                    "乘法次数（MAC）= O·O·K·K·Cᵢₙ·Cₒᵤₜ：O=16,K=3,Cᵢₙ=1,Cₒᵤₜ=3 → 16×16×9×3 = 6912。"
                    "对比全连接 256→256：参数 65792、MAC 65792。这就是「卷积省参数」的定量版本，"
                    "本页「各层参数量」柱状图会同时画两者。",
            "formula": "参数量 = K²·Cᵢₙ·Cₒᵤₜ + Cₒᵤₜ；　MAC = O²·K²·Cᵢₙ·Cₒᵤₜ",
        },
        {
            "title": "④ 池化与感受野",
            "body": "最大池化（max pooling）在 2×2 窗口里取最大值：把 [[1,7],[0,3]] 变成 7。"
                    "作用是降采样（尺寸减半）+ 轻微平移不敏感（最大值还在就还响应）。"
                    "注意它不引入参数。感受野递推：RFₗ = RFₗ₋₁ + (Kₗ − 1)·(Πⱼ<ₗ Sⱼ)。"
                    "三层 3×3、步长都为 1：1 → 3 → 5 → 7，即最后那个神经元能「看到」7×7 的原图区域。"
                    "中间夹 stride=2 的池化会让系数翻倍，感受野涨得极快——这解释了为什么深层 CNN 的"
                    "最后一层神经元对应原图整片区域，而分类器只需要一个全局平均池化（GAP）。",
            "formula": "RFₗ = RFₗ₋₁ + (Kₗ − 1)·Πⱼ<ₗ Sⱼ　（全 3×3、S=1 时就是 2L+1）",
        },
        {
            "title": "⑤ 卷积核里的数是怎么学到的",
            "body": "和 MLP 完全一样：反向传播。差别在于「共享」让梯度变成累加——"
                    "核里某个位置 k[a,b] 被用在每个滑动位置，所以它的梯度是所有位置回传的贡献之和："
                    "∂L/∂k[a,b] = Σₒ δₒ · x[i+a, j+b]。上面那个竖直边缘例子若真实标签要求「这里不该响应」，"
                    "每个检出边缘的位置都会推 k 变小 → 核逐渐学会只对更特定的模式响应。"
                    "至于输入方向的梯度 ∂L/∂x，则是用同一批核「转置卷积」式地往回散布——"
                    "这正是可视化「网络在看什么」（类激活图、梯度热力图）的基础。",
            "formula": "∂L/∂k = Σₒ δₒ ⊛ᶠˡᶦᵖ ᵀ x　（对核求和，对输入做全卷积转置）",
        },
    ],
    "terms": [
        {"term": "卷积核", "full": "Kernel / Filter", "explain": "一小块可学习权重（如 3×3），在输入上滑动做点积；一个 filter 产出一张特征图。"},
        {"term": "特征图", "full": "Feature Map / Activation Map", "explain": "某通道的输出矩阵，每个格子是「该位置对该核的响应强度」。"},
        {"term": "步长", "full": "Stride", "explain": "窗口每次滑动的格数；S=2 让输出边长减半，兼当下采样。"},
        {"term": "零填充", "full": "Padding", "explain": "边缘补 0 以保住尺寸（same 卷积），或不补（valid 卷积，每层缩 K−1）。"},
        {"term": "通道", "full": "Channel", "explain": "特征图的「层数」：图像是 R/G/B，中间层是不同检测器的输出。"},
        {"term": "局部感受野", "full": "Receptive Field", "explain": "某输出神经元对应到原输入上能「看到」的区域；随深度与步长累积扩大。"},
        {"term": "权值共享", "full": "Weight Sharing", "explain": "同一个核在空间各位置复用：参数大减 + 平移等变性的来源。"},
        {"term": "平移等变性", "full": "Translation Equivariance", "explain": "输入平移 → 特征图同幅平移（不是不变）；池化与全局平均才带来不变性。"},
        {"term": "池化", "full": "Max / Average Pooling", "explain": "无参数的局部聚合：max 取最强响应（保留「有没有」），avg 取平均（保留「整体亮度」）。"},
        {"term": "1×1 卷积", "full": "Pointwise Convolution / Bottleneck", "explain": "只混合通道不混合空间，用于升降通道数（Inception/ResNet 瓶颈层的省参数手法）。"},
        {"term": "空洞卷积", "full": "Dilated / Atrous Convolution", "explain": "核内插空格，在不增参数的前提下扩大感受野（语义分割常用）。"},
        {"term": "感受野失衡", "full": "Gridding Effect", "explain": "空洞率过大时响应呈网格状，丢掉连续性——大感受野不是免费的。"},
    ],
    "presets": [
        {"id": "same", "label": "保住尺寸（K=3,P=1,S=1）", "params": {"kernel": 3, "stride": 1, "pad": 1, "filters": 3}},
        {"id": "down", "label": "激进下采样（S=2,P=0）", "params": {"kernel": 3, "stride": 2, "pad": 0, "filters": 4}},
        {"id": "big", "label": "大核模糊细节（K=7）", "params": {"kernel": 7, "stride": 1, "pad": 3, "filters": 2}},
    ],
    "pitfalls": [
        "现象：几层之后特征图缩成 2×2、准确率跳水 → 忘了 padding 或 stride 叠太多。"
        "对策：先手算 O = ⌊(I−K+2P)/S⌋+1 再搭网络；本站「输出边长/参数量」曲线就是给你验算用的。",
        "现象：把输入通道数写错导致结果「灰蒙蒙」或形状对不上 → 3×3 核必须按 Cᵢₙ 复制成 K×K×Cᵢₙ 的块，"
        "点积跨通道累加成一张图。对策：记牢「一个 filter = 一整叠核，产出一张特征图」。",
        "现象：小图（32×32）用 ImageNet 的 7×7 步长 2  stem，一上来就 16×16，细节全丢 → 早期 stride 是元凶。"
        "对策：小数据集用 3×3 步长 1 起步，最后再下采样（CIFAR 类骨干的惯例）。",
        "现象：核尺寸一直加到 7、9，训练反而变差 → 大核参数多、易过拟合且优化更难。"
        "对策：堆小核，或用「大核 + 1×1 瓶颈」分解（Inception），或用空洞卷积换感受野。",
        "现象：网络完全学不动、损失卡在 log(类别数) → 常见于未初始化好 + 归一化缺失 + ReLU 大面积死亡。"
        "对策：He 初始化、加 BatchNorm、检查输入是否归一化到 0 附近（图像惯例是减均值除标准差）。",
        "现象：换旋转/尺度后的测试图准确率暴跌 → CNN 的平移等变不等于旋转/尺度不变。"
        "对策：数据增强（旋转、缩放、翻折），或改用可变形卷积/空间变换层。",
    ],
    "seeAlso": ["mlp", "transformer", "normalization", "optim", "biasvariance"],
    "quiz": {
        "q": "输入 32×32、Cᵢₙ=8，先用一个 3×3/Cₒᵤₜ=16/stride=1/padding=1 的卷积，再接 2×2 max pooling（stride=2）。输出形状与新增参数量是？",
        "options": [
            "32×32×16，参数量 1168",
            "16×16×16，参数量 1168（池化不增加参数）",
            "16×16×16，参数量 3×3×8×16 = 1152（无偏置）",
            "14×14×16，参数量 1168",
        ],
        "answer": 1,
        "why": "卷积后 O = ⌊(32−3+2)/1⌋+1 = 32，通道 16；池化把 32×32 变 16×16，通道不变 → 16×16×16。"
               "参数量 = 3²·8·16 + 16 = 1152 + 16 = 1168，池化没有可学习参数。"
               "（选项 C 忘了偏置，D 忘了 padding=1 正是为了保住 32。）",
    },
}
